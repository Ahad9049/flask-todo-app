apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ngnix-app.fullname" . }}-test-connection"
  labels:
    {{- include "ngnix-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ngnix-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
